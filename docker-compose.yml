services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exan-web
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      PORT: "5000"
      FLASK_DEBUG: "0"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
